// Import necessary modules
import React from "react";// Example: replace with your footer component

const Layout = ({ children }) => {
  return (
    <div>
      <main className="container mx-auto px-4">{children}</main>
    </div>
  );
};

export default Layout;
